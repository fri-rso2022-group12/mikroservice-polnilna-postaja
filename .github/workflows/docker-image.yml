name: Docker Image CI

on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]

jobs:

  build:

    runs-on: ubuntu-latest
    env:
      username_param:${{ secrets.DB_USERNAME }}
      password_param:${{ secrets.DB_PASSWORD }}
      hostname_param:${{ secrets.DB_HOSTNAME }}
      port_param:${{ secrets.DB_PORT }}
      database_param:${{ secrets.$DB_DATABASE }}

    steps:
    - uses: actions/checkout@v3
    - name: Build the Docker image
      run: docker build . --file Dockerfile  $username_param $password_param $hostname_param $port_param $database_param --tag charging-station:$(date +%s)

